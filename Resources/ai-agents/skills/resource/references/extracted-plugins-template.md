# 🔌 Extracted Plugins: {{RESOURCE_NAME}}

> **Source:** `{{NAMESPACE}}`
> **Extraction Date:** {{DATE}}

<!-- ⚠️ AGENT INSTRUCTION: You MUST read `.para-workspace.yml` to get the user's preferred language (`preferences.language`). All generated text replacing the brackets [Agent: ...] MUST be translated into the user's preferred language. The section titles MAY remain in English if preferred. -->

## 1. Plugin Architecture Overview
- [Agent: Summarize the plugins found in this resource. How are they structured, what capabilities do they bundle, and what is their integration purpose?]

## 2. Plugin Manifests
### 2.1. [Agent: Plugin Name]
- **Original Location:** `[Agent: Folder location, e.g., plugins/my-plugin/]`
- **Manifest Content (`plugin.json`):**
```json
[Agent: Raw plugin.json content]
```
- **Associated Subcomponents:**
  - **Skills:** [Agent: List folder/paths of packaged skills]
  - **Rules:** [Agent: List folder/paths of packaged rules]
  - **MCP Configuration:** [Agent: Describe if it embeds an MCP server or has `mcp_config.json`]

## 3. Porting & Adaptation
- [Agent: Suggest how this plugin can be installed or registered in the current PARA Workspace (`plugins/` or `.agents/plugins/`).]
