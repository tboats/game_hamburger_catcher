# 📋 Extracted Workflows: {{RESOURCE_NAME}}

> **Source:** `{{NAMESPACE}}`
> **Extraction Date:** {{DATE}}

<!-- ⚠️ AGENT INSTRUCTION: You MUST read `.para-workspace.yml` to get the user's preferred language (`preferences.language`). All generated text replacing the brackets [Agent: ...] MUST be translated into the user's preferred language. The section titles MAY remain in English if preferred. -->

## 1. Workflow Engine & Sequence Overview
- [Agent: Summarize how the resource orchestrates multi-step processes or agent workflows. Does it use structured markdown workflows (like PARA), CLI commands, state machines, or durable queues?]

## 2. Workflow Teardown
### 2.1. [Agent: Workflow Name]
- **Original Location:** `[Agent: File path or CLI command trigger, e.g., .agents/workflows/my-workflow.md]`
- **Pre-flight & Prerequisites:** [Agent: Describe the conditions required before this workflow executes.]
- **Sequence Steps:**
1. [Agent: Step 1 description]
2. [Agent: Step 2 description]
- **Key Automations (e.g., // turbo):** [Agent: List any steps that are designed to run automatically with terminal commands.]

### 2.2. [Agent: Workflow Name]
- **Original Location:** `[Agent: File path]`
- **Sequence Steps:**
1. [Agent: Step description]
- **Key Automations:** [Agent: Description]

## 3. Porting & Adaptation
- [Agent: Suggest how these step-by-step workflows can be adapted into the current PARA Workspace workflows catalog (e.g., `.agents/workflows/`).]
